

function Sum(a, b){
    return a + b;
}

console.log(Sum(3, 4));

//This two are almost similar but not exactly.
const subtract = (a, b)=>{
    return a + b;
}

console.log(subtract(5, 6));

// app.get('/', function(req, res){

// });


// app.get('/', (req, res)=>{

// });